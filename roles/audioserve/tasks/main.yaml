# mpatton/audioserve
# https://github.com/izderadicka/audioserve

# /Media/Books/Audiobooks

docker run -d --name audioserve -p 3000:3000 \
    -v /path/to/your/audiobooks1:/collection1 \
    -v /path/to/your/audiobooks2:/collection2 \
    -e DIRS=/collection1 /collection2 \
    -e SECRET=mypassword \
    -e SSLKEY= \
    -e SSLPASS= \
    -e OTHER_ARGS=--search-cache \
    audioserve

  docker run -d --init --name audioserve -p 3000:3000 \
    -v /path/to/your/audiobooks1:/collection1 \
    -v /path/to/your/audiobooks2:/collection2 \
    audioserve \
    -s mypass \
    --ssl-key /audioserve/ssl/audioserve.p12 --ssl-key-password mypass \
    --search-cache \
    /collection1 /collection2


    docker run -d --init 
    --name audioserve 
    -p 3000:3000 
    -v /path/to/your/audiobooks:/audiobooks  
    izderadicka/audioserve  

version: 3
services:
    collection2:
        container_name: audioserve
        ports:
            - '3000:3000'
        volumes:
            - '/path/to/your/audiobooks1:/collection1'
            - '/path/to/your/audiobooks2:/collection2'
        image: /collection2
