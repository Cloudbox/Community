##########################################################################
# Title:         Cloudbox: plex2 - DB Cache Size Settings                 #
# Author(s):     desimaniac                                              #
# URL:           https://github.com/cloudbox/cloudbox                    #
# --                                                                     #
#         Part of the Cloudbox project: https://cloudbox.works           #
##########################################################################
#                   GNU General Public License v3.0                      #
##########################################################################
---
- name: Wait for plex2 DB to be created
  wait_for:
    path: "/opt/plex2/Library/Application Support/Plex Media Server/Plug-in Support/Databases/com.plexapp.plugins.library.db"
    state: present
    timeout: 600

- name: Get Current plex2 DB Cache Size
  shell: sqlite3 "/opt/plex2/Library/Application Support/Plex Media Server/Plug-in Support/Databases/com.plexapp.plugins.library.db" "PRAGMA default_cache_size;"
  args:
    executable: /bin/bash
  register: current_db_cache_size

- name: Set 'default_db_cache_size' variable
  set_fact:
    default_db_cache_size: '2000'

- name: Check if 'plex.db_cache_size' was specified
  set_fact:
    db_cache_size_is_specified: "{{ true if not (
        (plex.db_cache_size is undefined)
        or
        (plex.db_cache_size is none)
        or
        (plex.db_cache_size | trim | length == 0)
      )
      else false }}"

- name: Get Desired Plex DB Cache Size
  set_fact:
    desired_db_cache_size: "{{ default_db_cache_size
      if ( (not db_cache_size_is_specified) or (plex.db_cache_size == 'default') )
      else ( (plex.db_cache_size) | regex_replace(',', '') | int ) }}"

- name: Make plex2 DB Edits
  block:

  - name: Stop plex2 Container
    docker_container:
      name: plex2
      state: stopped

  - name: Display Desired Plex DB Cache Size
    debug:
      msg: "Desired Plex DB Cache Size is '{{ desired_db_cache_size }}'"

  - name: "Set plex2 DB Cache Size"
    shell: |
      sqlite3 "/opt/plex2/Library/Application Support/Plex Media Server/Plug-in Support/Databases/com.plexapp.plugins.library.db" \
        "PRAGMA default_cache_size = '{{ desired_db_cache_size | int }}';"
    args:
      executable: /bin/bash

  - name: Get Current plex2 DB Cache Size
    shell: |
      sqlite3 "/opt/plex2/Library/Application Support/Plex Media Server/Plug-in Support/Databases/com.plexapp.plugins.library.db" \
        "PRAGMA default_cache_size;"
    args:
      executable: /bin/bash
    register: new_db_cache_size

  - name: Display Current plex2 DB Cache Size
    debug:
      msg: "Plex2 DB cache size is now set to '{{ new_db_cache_size.stdout | int }}'."

  - name: Start plex2 Container
    docker_container:
      name: plex2
      state: started

  when: ( current_db_cache_size.stdout | int ) != ( desired_db_cache_size | regex_replace(',', '') | int )
