#########################################################################
# Title:            Cloudbox Community: Couchpota                       #
# Author(s):        javi11                                              #
# URL:              https://github.com/Cloudbox/Community               #
# Docker Image(s):  jlesage/filebot                                     #
# --                                                                    #
#         Part of the Cloudbox project: https://cloudbox.works          #
#########################################################################
#                   GNU General Public License v3.0                     #
#########################################################################
---
- name: 'Setting CloudFlare DNS Record'
  include_role:
    name: cloudflare-dns
  vars:
    subdomain: couchpota
  when: cloudflare_enabled

- name: Stop and remove any existing container
  docker_container:
    name: couchpota
    state: absent

- name: Create couchpota directories
  file: 'path={{ item }} state=directory mode=0775 owner={{ user.name }} group={{ user.name }}'
  with_items:
    - /opt/couchpota

- name: Create and start container
  docker_container:
    name: couchpota
    image: linuxserver/couchpotato
    pull: yes
    published_ports:
      - '127.0.0.1:5050:5050'
    env:
      UMASK_SET: 022
      UMASK: 002
      TZ: 'Europe/London'
      PUID: '{{ uid }}'
      PGID: '{{ gid }}'
      VIRTUAL_HOST: 'couchpota.{{ user.domain }}'
      VIRTUAL_PORT: 5050
      LETSENCRYPT_HOST: 'couchpota.{{ user.domain }}'
      LETSENCRYPT_EMAIL: '{{ user.email }}'
    volumes:
      - '/etc/localtime:/etc/localtime:ro'
      - '/opt/couchpota:/config'
      - '/mnt:/mnt'
      - '/mnt/unionfs/Media/Movies:/movies'
      - '/mnt/local/downloads:/downloads'
    labels:
      'com.github.cloudbox.cloudbox_managed': 'true'
    networks:
      - name: cloudbox
        aliases:
          - couchpota
    purge_networks: yes
    restart_policy: always
    state: started
